# 🚀 Деплой на GitHub Pages - Пошаговая инструкция

## 📋 Что нужно сделать

1. Создать репозиторий на GitHub
2. Загрузить код в репозиторий
3. Настроить GitHub Pages
4. Получить URL и настроить Telegram Mini App

---

## Шаг 1: Создание репозитория на GitHub

### 1.1. Создайте новый репозиторий

1. Откройте [GitHub](https://github.com) и войдите в аккаунт
2. Нажмите на кнопку **"+"** в правом верхнем углу → **"New repository"**
3. Заполните форму:
   - **Repository name**: `debt-manager` (или любое другое название)
   - **Description**: "Telegram Mini App для управления финансами"
   - **Visibility**: Public (для бесплатного GitHub Pages) или Private
   - **НЕ** ставьте галочки на "Add a README file", "Add .gitignore", "Choose a license"
4. Нажмите **"Create repository"**

### 1.2. Скопируйте URL репозитория

После создания репозитория GitHub покажет вам URL. Он будет выглядеть так:
```
https://github.com/ваш-username/debt-manager.git
```

**Сохраните этот URL!** Он понадобится в следующем шаге.

---

## Шаг 2: Настройка проекта

### 2.1. Обновите base path в vite.config.ts

**Важно:** Если название вашего репозитория НЕ `debt-manager`, нужно обновить `base` в `vite.config.ts`:

1. Откройте файл `vite.config.ts`
2. Найдите строку:
   ```typescript
   base: process.env.NODE_ENV === 'production' ? '/debt-manager/' : '/',
   ```
3. Замените `'/debt-manager/'` на `'/ваше-название-репозитория/'`

Например, если репозиторий называется `finance-app`, то:
```typescript
base: process.env.NODE_ENV === 'production' ? '/finance-app/' : '/',
```

### 2.2. Проверьте наличие файлов

Убедитесь, что в проекте есть:
- ✅ `.github/workflows/deploy.yml` (уже создан)
- ✅ `.gitignore` (уже есть)
- ✅ `vite.config.ts` (обновлён)

---

## Шаг 3: Загрузка кода в GitHub

### Вариант A: Через командную строку (рекомендуется)

Откройте терминал (PowerShell) в папке проекта и выполните:

```bash
# Перейдите в папку проекта
cd "C:\Users\USER\Desktop\debt-manager"

# Инициализируйте git (если еще не сделано)
git init

# Добавьте все файлы
git add .

# Сделайте первый коммит
git commit -m "Initial commit: Telegram Mini App"

# Добавьте remote репозиторий (замените URL на ваш!)
git remote add origin https://github.com/ваш-username/debt-manager.git

# Загрузите код в GitHub
git branch -M main
git push -u origin main
```

**Если Git запросит авторизацию:**
- Используйте Personal Access Token вместо пароля
- Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic) → Generate new token
- Выберите scope: `repo` (полный доступ к репозиториям)

### Вариант B: Через GitHub Desktop

1. Скачайте и установите [GitHub Desktop](https://desktop.github.com/)
2. Откройте GitHub Desktop
3. File → Add Local Repository
4. Выберите папку `C:\Users\USER\Desktop\debt-manager`
5. Нажмите "Publish repository"
6. Выберите репозиторий и нажмите "Publish repository"

### Вариант C: Через VS Code

1. Откройте проект в VS Code
2. Нажмите на иконку Git в боковой панели
3. Нажмите "Initialize Repository"
4. Добавьте все файлы (нажмите "+" рядом с файлами)
5. Введите сообщение коммита: "Initial commit"
6. Нажмите "Commit"
7. Нажмите "Publish Branch"
8. Выберите репозиторий на GitHub

---

## Шаг 4: Настройка GitHub Pages

### 4.1. Включите GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Перейдите в **Settings** (вкладка вверху)
3. В левом меню найдите **Pages**
4. В разделе **"Source"** выберите:
   - **Source**: `GitHub Actions`
5. Нажмите **Save**

### 4.2. Запустите деплой

GitHub Actions автоматически запустится после первого push. Но вы можете запустить его вручную:

1. Перейдите во вкладку **Actions** в вашем репозитории
2. Выберите workflow **"Deploy to GitHub Pages"**
3. Нажмите **"Run workflow"** → **"Run workflow"**

### 4.3. Дождитесь завершения деплоя

1. Во вкладке **Actions** вы увидите процесс деплоя
2. Дождитесь завершения (зелёная галочка)
3. Это может занять 2-5 минут

---

## Шаг 5: Получение URL приложения

После успешного деплоя:

1. Перейдите в **Settings** → **Pages**
2. Вы увидите URL вашего приложения:
   ```
   https://ваш-username.github.io/debt-manager/
   ```
   (или `https://ваш-username.github.io/ваше-название-репозитория/`)

3. **Скопируйте этот URL!**

### Проверка работы

Откройте URL в браузере. Вы должны увидеть ваше приложение.

---

## Шаг 6: Настройка Telegram Mini App

Теперь нужно указать этот URL в BotFather:

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте `/myapps`
3. Выберите вашего бота
4. Выберите ваше Mini App
5. Отправьте `/editapp`
6. Выберите **"Web App URL"**
7. Вставьте URL из шага 5 (например: `https://ваш-username.github.io/debt-manager/`)
8. Готово! 🎉

---

## 🔄 Обновление приложения

После каждого изменения кода:

```bash
# Добавьте изменения
git add .

# Сделайте коммит
git commit -m "Описание изменений"

# Загрузите на GitHub
git push
```

GitHub Actions автоматически задеплоит новую версию на GitHub Pages (обычно 2-5 минут).

---

## 🐛 Решение проблем

### Проблема: Страница показывает 404

**Решение:**
1. Проверьте, что в `vite.config.ts` указано правильное название репозитория в `base`
2. Убедитесь, что деплой завершился успешно (зелёная галочка в Actions)
3. Подождите несколько минут (GitHub Pages может обновляться с задержкой)

### Проблема: GitHub Actions не запускается

**Решение:**
1. Проверьте, что файл `.github/workflows/deploy.yml` существует
2. Убедитесь, что вы запушили код в ветку `main` или `master`
3. Проверьте, что GitHub Pages настроен на использование GitHub Actions

### Проблема: Приложение не работает в Telegram

**Решение:**
1. Убедитесь, что URL правильный и начинается с `https://`
2. Проверьте, что URL заканчивается на `/` (например: `https://username.github.io/debt-manager/`)
3. Откройте URL в браузере и убедитесь, что приложение работает
4. Проверьте консоль браузера на наличие ошибок (F12)

### Проблема: Стили не загружаются

**Решение:**
1. Проверьте `base` в `vite.config.ts` - он должен соответствовать названию репозитория
2. Очистите кэш браузера
3. Пересоберите проект: `npm run build`

### Проблема: Git требует авторизацию

**Решение:**
1. Используйте Personal Access Token вместо пароля
2. Создайте токен: GitHub → Settings → Developer settings → Personal access tokens
3. Выберите scope: `repo` (полный доступ)
4. Используйте токен как пароль при push

---

## 📝 Полезные команды Git

```bash
# Проверить статус
git status

# Посмотреть историю коммитов
git log

# Отменить последний коммит (но оставить изменения)
git reset --soft HEAD~1

# Посмотреть удалённые репозитории
git remote -v

# Изменить URL удалённого репозитория
git remote set-url origin https://github.com/новый-url.git
```

---

## ✅ Чек-лист

Перед тем как считать деплой завершённым, проверьте:

- [ ] Репозиторий создан на GitHub
- [ ] Код загружен в репозиторий
- [ ] `base` в `vite.config.ts` соответствует названию репозитория
- [ ] GitHub Pages включён и настроен на GitHub Actions
- [ ] Деплой завершился успешно (зелёная галочка в Actions)
- [ ] Приложение открывается по URL в браузере
- [ ] URL указан в BotFather для Mini App
- [ ] Приложение открывается в Telegram

---

## 🎉 Готово!

Ваше приложение теперь доступно по адресу:
```
https://ваш-username.github.io/debt-manager/
```

И работает как Telegram Mini App! 🚀

---

## 📚 Дополнительная информация

- [Документация GitHub Pages](https://docs.github.com/en/pages)
- [Документация GitHub Actions](https://docs.github.com/en/actions)
- [Документация Vite](https://vitejs.dev/guide/static-deploy.html#github-pages)


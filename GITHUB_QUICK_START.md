# ⚡ Быстрый старт - GitHub Pages

## 🚀 За 5 минут

### 1. Создайте репозиторий на GitHub
- Откройте https://github.com/new
- Название: `debt-manager` (или любое другое)
- Public репозиторий
- **НЕ** добавляйте README, .gitignore, лицензию
- Нажмите "Create repository"

### 2. Обновите base path (если нужно)

Если название репозитория НЕ `debt-manager`, откройте `vite.config.ts` и замените:
```typescript
base: process.env.NODE_ENV === 'production' ? '/ваше-название/' : '/',
```

### 3. Загрузите код

```bash
cd "C:\Users\USER\Desktop\debt-manager"
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/ваш-username/debt-manager.git
git push -u origin main
```

### 4. Включите GitHub Pages

1. GitHub → ваш репозиторий → **Settings** → **Pages**
2. Source: **GitHub Actions**
3. Сохраните

### 5. Дождитесь деплоя

- Перейдите в **Actions**
- Дождитесь зелёной галочки (2-5 минут)
- URL будет: `https://ваш-username.github.io/debt-manager/`

### 6. Настройте Telegram

1. BotFather → `/myapps` → ваш бот → ваше Mini App
2. `/editapp` → "Web App URL"
3. Вставьте URL из шага 5
4. Готово! 🎉

---

## ❓ Проблемы?

**404 ошибка?**
- Проверьте `base` в `vite.config.ts`
- Убедитесь, что деплой завершился успешно

**Не работает в Telegram?**
- URL должен начинаться с `https://`
- URL должен заканчиваться на `/`
- Откройте URL в браузере - должно работать

**Полная инструкция:** [GITHUB_PAGES_DEPLOY.md](./GITHUB_PAGES_DEPLOY.md)

